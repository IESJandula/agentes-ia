services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: chroma_local
    ports:
      - "8000:8000"
    volumes:
      - ./chroma_data:/chromadb/data
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE
    restart: unless-stopped
